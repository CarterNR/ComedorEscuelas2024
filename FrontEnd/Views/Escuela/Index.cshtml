@model IEnumerable<FrontEnd.Models.EscuelaViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdEscuela)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NombreEscuela)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.IdEscuela)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NombreEscuela)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Estado)
                </td>
                <td>

                    @Html.ActionLink("Edit", "Edit", new { id = item.IdEscuela }) |
                    @Html.ActionLink("Details", "Details", new { id = item.IdEscuela }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.IdEscuela })
                </td>
            </tr>
        }
    </tbody>
</table>
